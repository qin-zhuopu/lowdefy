name: Companies

dependencies:
  - id: contacts
    description: Contact management

components:
  - id: company-selector
    type: Component
    component:
      _ref: components/company-selector.yaml

pages:
  - _ref: pages/company-detail.yaml
