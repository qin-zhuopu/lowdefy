name: Contacts

vars:
  label:
    type: string
    default: Contacts

dependencies:
  - id: companies

components:
  - id: contact-selector
    type: Component
    component:
      _ref: components/contact-selector.yaml

pages:
  - _ref: pages/contact-detail.yaml
